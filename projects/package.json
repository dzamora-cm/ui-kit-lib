{
  "name": "@jgarcia-cm/ui-kit-lib",
  "version": "0.0.7",
  "peerDependencies": {
    "@angular/common": "^13.3.0",
    "@angular/core": "^13.3.0"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "scripts": {
    "prepare": "npm run co:login",
    "co:login": "aws codeartifact login --tool npm --repository package-test --domain jgarcia",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
